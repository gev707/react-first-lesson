export default function User({name,surname,age}) {
    return (
        <div>
            <p>User-name : {name}</p>
            <p>User-surname : {surname}</p>
            <p>User-age : {age}</p>
        </div>
    )
}